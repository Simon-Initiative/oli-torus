import React from 'react';
export const BlockQuoteEditor = (props) => {
    const { attributes, children } = props;
    return <blockquote {...attributes}>{children}</blockquote>;
};
//# sourceMappingURL=Editor.jsx.map