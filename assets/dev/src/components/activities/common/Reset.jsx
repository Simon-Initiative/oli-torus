import React from 'react';
export const Reset = ({ onClick, hasMoreAttempts, }) => (<button disabled={!hasMoreAttempts} onClick={onClick} className="btn btn-sm btn-primary muted">
    Reset
  </button>);
//# sourceMappingURL=Reset.jsx.map