import React from 'react';
import { useDispatch } from 'react-redux';

const FabricCanvas: React.FC<any> = (props) => {
  return <canvas className="aa-canvas-inner"></canvas>;
};

export default FabricCanvas;
