<div class="container">
  <span>
    <%= link to: Routes.brand_path(@conn, :index) do %>
      <i class="las la-arrow-left"></i> Back
    <% end %>
  </span>

  <%= render OliWeb.SharedView, "_box_form_container.html", Map.put(assigns, :title, "Create Brand") do %>
    <%= render "form.html", Map.put(assigns, :action, Routes.brand_path(@conn, :create)) %>
  <% end %>
</div>
