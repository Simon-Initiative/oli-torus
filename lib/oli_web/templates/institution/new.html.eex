<div class="container">
  <span>
    <%= link to: Routes.live_path(OliWeb.Endpoint, OliWeb.Admin.Institutions.IndexLive) do %>
      <i class="fas fa-arrow-left"></i> Back
    <% end %>
  </span>

  <%= render OliWeb.SharedView, "_box_form_container.html", Map.put(assigns, :title, "Register Institution") do %>
    <%= render "form.html", Map.put(assigns, :action, Routes.institution_path(@conn, :create)) %>
  <% end %>
</div>
