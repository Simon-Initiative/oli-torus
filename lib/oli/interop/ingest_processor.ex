defmodule Oli.Interop.IngestProcessor do
  alias Oli.Interop.IngestState

  def process(%IngestState{} = state, as_author) do
    # Create project, pub

    # Bulk record create

    # Revision edit by type

    # rewiring
  end
end
