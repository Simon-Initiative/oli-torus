# Gaps — email_sending (MER-5257)

Open questions and decisions needed before or during implementation. Document captures items the Jira ticket, Figma design, and codebase token system do not unambiguously specify. Type A (paperwork transcription gaps where Jira already has the answer but `prd.md` / `requirements.yml` did not capture it) are NOT tracked here — those are applied directly as edits to those files. This document tracks only items that require a real decision from a third party (product, design, or tech lead).

## Reference links

- Jira ticket: https://eliterate.atlassian.net/browse/MER-5257
- Parent epic: https://eliterate.atlassian.net/browse/MER-5198
- Figma node (modal — dark variant): https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=955-17500
- Token file: `assets/tailwind.tokens.js`
- Reporter: Jess Fortunato
- Tech blueprint author: Darren Siegel (Jira comment 1)

## Status legend

- `OPEN` — not yet raised with owner
- `PROPOSED` — codebase research has produced a defensible answer, but the answer requires owner confirmation before locking (e.g., policy or operational thresholds)
- `ASKED` — raised with owner, waiting on response
- `ANSWERED` — response received, needs PRD/FDD/code update
- `RESOLVED` — decision applied to docs/code, gap closed

## B1 — Jira scope gaps (owner: Jess Fortunato)

Jira ticket: https://eliterate.atlassian.net/browse/MER-5257

| ID | Gap | Source | URL | Status | Decision |
|----|-----|--------|-----|--------|----------|
| G-J01 | Jira phrasing: _"Any other supported location where emailing students is applicable."_ Final list of entry points beyond Student Support, Assessments, Student Overview, Content→Student list, LO→Student list. Implementation needs a closed enumeration. | Jira AC §Context-Aware Entry Points | [MER-5257](https://eliterate.atlassian.net/browse/MER-5257) | RESOLVED | Resolved 2026-05-04 by exhaustive codebase + spec search. Closed list = the 5 explicit Jira entry points: Student Support tile, Assessments tile, Student Overview, Content → Student list, Learning Objectives → Student list. No additional dashboard email entry points exist in code or specs. Legacy `manage/students` email mechanism is out of scope (course-management, not dashboard). |
| G-J02 | Situation enum — full canonical key list (Darren proposed `:incomplete_assessment`, `:struggling_students` as samples). Need full set + canonical human-readable description per key for prompt assembly. | Darren comment 1, point 4 | [Darren comment](https://eliterate.atlassian.net/browse/MER-5257?focusedCommentId=43980) | RESOLVED | Resolved 2026-05-04 by deriving keys from existing tile projectors and PRDs. Closed list (7 keys): `:struggling_students`, `:active_students_on_track`, `:excelling_students`, `:inactive_students` (Student Support bucket atoms — `student_support/projector.ex:6`); `:incomplete_assessment` (`assessments/projector.ex` completion_status); `:low_proficiency_objectives` (Challenging Objectives tile ≤ 40% proficiency); `:beginning_course` (ai_infra fallback when no actionable signal — MEDIUM confidence, validate during Phase 1 implementation). Each key derives from an existing canonical code/spec taxonomy; no product invention required. |
| G-J03 | Jira phrasing: _"Any applicable deadlines or performance signals."_ Need exact fields that qualify (e.g., score, attempt count, time on task, last login). Context builder needs concrete shape. | Jira AC §Context-Aware Entry Points; Darren comment 1, point 3 | [MER-5257](https://eliterate.atlassian.net/browse/MER-5257) | RESOLVED | Resolved 2026-05-04 by deriving fields from existing projector `@type` definitions. Performance signals: `progress_pct`, `proficiency_pct`, `completion_ratio`, `completion_status`, `mean_score`, `median_score`, `proficiency_label`, `activity_status`, `bucket.{count, active_count, inactive_count}`. Deadlines/time: `due_at`, `available_at`, `last_interaction_at`. Cross-cutting: `section_id`, `course_title`, `scope_label`. Per-recipient: `student_id`, `email`, `given_name`, `family_name`. Per-assessment adds `title`, `histogram`. Per-objective adds `title`, `proficiency_distribution`. Per-content-item adds `title`, `label`, `resource_type`. All HIGH confidence; all derived from `student_support/projector.ex:128-131`, `assessments/projector.ex:60-76`, `challenging_objectives.ex:229-235`, `progress/projector.ex:67-72`. Proposed `Oli.InstructorDashboard.EmailContext` struct documented in research deliverable. |
| G-J04 | Per-recipient send fan-out — partial-fail behavior. If 7 of 10 recipient jobs enqueue successfully and 3 fail validation, what is the expected result? Block all? Send 7 and surface 3? | Darren comment 1, point 9 (says "no silent partial success" — needs UX policy) | [Darren comment](https://eliterate.atlassian.net/browse/MER-5257?focusedCommentId=43980) | RESOLVED | Resolved 2026-05-04. **Send valid recipients + notify on failures (no block-all).** Aligns with Darren's "no silent partial success" constraint. **Backend:** wrap `Oban.insert_all/2` to validate returned count vs input count; collect per-recipient failure reasons (invalid email, enqueue failure, validation error). **UI:** post-send notification surfaces "X emails failed" with the failed recipients listed + reason for each. **Out of scope this iteration:** retry / re-send affordance — purely informational this iteration. Future iteration can add re-send UI if observed need. Existing Torus fan-out flows (mailer, certificates, embeddings, enrollments) currently do silent partial success — MER-5257 is the first feature to break this pattern intentionally. |
| G-J05 | "Required email fields invalid" — what counts as "required" beyond recipients? Subject? Body non-empty? | Jira negative AC | [MER-5257](https://eliterate.atlassian.net/browse/MER-5257) | RESOLVED | Resolved 2026-05-04 from existing `student_support_email_modal.ex:312-316` precedent. Required for valid send: (1) recipient count > 0, (2) `subject.trim() != ""`, (3) `body.trim() != ""`. From/Reply-To/HTML body are auto-derived. No length limits exist in code; defer to Swoosh/SMTP implicit limits unless product wants explicit caps. |
| G-J06 | Max recipient count per send — operational safety bound. | PRD §14 Open Question OQ-2 | [prd.md §14](prd.md) | RESOLVED | Resolved 2026-05-04. **Configurable cap via env var.** New env var `INSTRUCTOR_EMAIL_MAX_RECIPIENTS` read at runtime in `config/runtime.exs` (matches existing `OBAN_QUEUE_SIZE_MAILER` pattern). **Default = 100** (conservative; supports typical instructor cohort sends, leaves headroom under Oban mailer throughput of ≈3 emails/sec). Deployments without explicit override use the default (fail-closed). **UI behavior at cap:** block Send + show inline error naming the current cap (e.g., "X recipients selected, max N. Split into multiple sends or reduce selection.") with the cap value sourced from server-side config so it always matches actual enforcement. **⟶ REVERSED 2026-05-25 (Session 11): the `INSTRUCTOR_EMAIL_MAX_RECIPIENTS` cap was DROPPED before implementation — no existing Torus email send caps recipients, so the feature follows that convention (no cap). Revisit only if abuse surfaces. Final state: no recipient cap; the chip list renders all recipients with client-side `OverflowChipList` overflow.** |
| G-J07 | Manual recipients — restricted to enrolled section learners only, or arbitrary external email addresses allowed? Significant authorization implication. | PRD §14 Open Question OQ-1 | [prd.md §14](prd.md) | RESOLVED | Resolved 2026-05-05 by Jess (Slack confirmation). **Section-enrolled students only.** Jess: _"I assume that they would only be emailing students in the section so let's start with that. They can only see emails for students in their sections anyways (in student list)."_ Manual-add UI = section-scoped student picker, NOT free-text email entry. G-D08 implementation follows this scope. |
| G-J08 | Fallback copy when generation fails before any draft is produced — exact UX text. | PRD §14 Open Question OQ-3 | [prd.md §14](prd.md) | RESOLVED | Resolved 2026-05-05 by Jess (Slack confirmation: _"yes this all sounds fine!"_). Final wording: _"We couldn't generate a draft right now. Try again or write your own."_ Modal stays open per PRD §8 reliability requirement; retry available per Darren comment 9. |
| G-J09 | Mobile / responsive scope — is the modal expected to render on mobile breakpoints in this iteration, or desktop-only? | not specified anywhere | [MER-5257](https://eliterate.atlassian.net/browse/MER-5257) | RESOLVED | Resolved 2026-05-04 by sibling tile precedent. Shipped tiles (Summary `summary_tile.ex:77-79`, Student Support `student_support_tile.ex:193,226-227`, Parameters Modal `student_support_parameters_modal.ex:14-16,20-21`) all use Tailwind responsive prefixes (`sm:` `md:` `lg:` `xl:` `2xl:`). Base Modal component default wrapper = `w-full p-4 sm:p-6 lg:py-8` (`modal.ex:32`). **Decision:** MER-5257 modal follows the same responsive pattern: adaptive padding `px-4 sm:px-6 md:px-10 lg:px-16` and width `max-w-full sm:max-w-[90vw] md:max-w-[1048px]`. Existing static Email Modal (`student_support_email_modal.ex:36-38`) is an outlier — do not propagate. |
| G-J10 | i18n — are modal labels, validation messages, tone labels, and footnote text in scope for translation in this iteration, or English-only first? | PRD §5 mentions externalization but not iteration scope | [prd.md §5](prd.md) | RESOLVED | Resolved 2026-05-04 by tile precedent. Every shipped intelligent-dashboard tile renders hard-coded English with zero `gettext` wrapping (`progress_tile.ex`, `summary_tile.ex:14-19`, `student_support_tile.ex`, `assessments_tile.ex`, `student_support_email_modal.ex`). No instructor-dashboard `.po` files exist in `priv/gettext/`. **Decision:** MER-5257 follows precedent — hard-coded English labels in this iteration. Future cross-tile i18n refactor would be a separate epic. PRD §5 "externalized/localizable" wording should be revised to "future-i18n-friendly (English-only this iteration)." |
| G-J11 | Feature flag for rollout — PRD §11 says "no feature flags." Confirm intentional given high entry-point surface and AI cost. | PRD §11 | [prd.md §11](prd.md) | RESOLVED | Resolved 2026-05-04 — **no feature flag.** Note: this gap was originally surfaced as second-guessing of PRD §11 ("No feature flags present in this feature"), not a Jira/Darren spec requirement. PRD §11 is trusted. Ship without flag. Revisit only if production rollout pain observed. |
| G-J12 | AI quota / cost — covered by the new GenAI Feature Config "Instructor Email"? Need confirmation that per-section / per-instructor rate limits will live there. | Darren comment 1, point 10 | [Darren comment](https://eliterate.atlassian.net/browse/MER-5257?focusedCommentId=43980) | RESOLVED | Resolved 2026-05-04 — Option C (deferred). Note: this gap was originally surfaced as a forward-looking concern, not a Jira/Darren spec requirement. Darren's comment 10 only requires a new GenAI Feature Config (not per-section quotas). **Decision:** rely on existing global `RegisteredModel.max_concurrent` (`registered_model.ex:16`) for protection. No new per-section rate-limit fields added to `ServiceConfig` / `FeatureConfig` this iteration. Revisit if abuse signals appear in production telemetry. |

## B2 — Figma design state gaps (owner: design team / Jess)

Figma node URL (modal — dark variant): https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=955-17500

Figma node provides one state only (dark mode, default content). The following are not designed and need design input or a documented fallback decision.

| ID | Gap | Notes | URL | Status | Decision |
|----|-----|-------|-----|--------|----------|
| G-D01 | Light mode variant — modal currently designed in dark mode only (background `#0d0c0f`). App ships both modes. | Verified 2026-05-04 by full Figma file scan (canvas "Handoff" 895:8350, section "Email" 902:4302). Section contains exactly 2 frames: "Support Email" (`955:17500`, dark) and "Assignment Email" (`1115:18333`, also dark). No light variant exists for either. Canvas "Light & Dark Mode" exists separately but contains dashboard layouts, not email modals. | [Figma section "Email" 902:4302](https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=902-4302) | RESOLVED | Resolved 2026-05-05 — **not a real gap.** Light mode is automatically derived from the codebase token system. Every Figma color used in the modal maps to a named token in `assets/tailwind.tokens.js`, and each token already pairs a `light` and `dark` hex variant. As long as the implementation uses token classes (e.g., `bg-Background-bg-primary`, `text-Text-text-high`) rather than hard-coded hex, Tailwind auto-resolves the correct variant per theme. Verified 13 of 16 Figma colors map cleanly to tokens with both variants present; the remaining 3 are tracked under B3 (G-T01 / G-T02 / G-T03) and are independently resolved. The only remaining color authority is `Fill-Buttons-fill-primary-bold` (new token added per G-T01). Implementation must use token classes consistently — no raw hex from the Figma export. |
| G-D02 | Hover, focus, and disabled states for all buttons (Generate New Draft, tone buttons, Send, Cancel, X close, chip remove X). | not designed | [Figma node](https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=955-17500) | RESOLVED | Resolved 2026-05-04 from existing button primitives `lib/oli_web/components/design_tokens/primitives/button.ex:519-612`. Use the `Button.button` primitive for all standard buttons. Patterns: hover = `hover:bg-Fill-Buttons-fill-primary-hover` (primary), `hover:border-Border-border-bold-hover hover:bg-Surface-surface-secondary-hover` (secondary), `hover:underline` (text); focus = `focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-Fill-Buttons-fill-primary`; disabled = `cursor-not-allowed focus-visible:outline-none` + variant-specific muted bg. Chip remove-X = `inline-flex h-4 w-4 items-center justify-center opacity-60 hover:opacity-100 focus:outline-2 focus:outline-Fill-Buttons-fill-primary`. |
| G-D03 | Loading state during AI draft generation (initial open + regenerate). | not designed | [Figma node](https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=955-17500) | RESOLVED | Resolved 2026-05-04 from sibling tile pattern `summary_tile.ex:190-198`. Use `Icons.ai_spinner` (animated SVG, 28x28) + text "Generating draft..." in a status box, wrapped in `<span role="status" aria-live="polite">`. Disable Generate button during request via `phx-disable-with="Generating..."`. No custom skeleton component needed. |
| G-D04 | AI generation failure UI — retry pattern, error message position and copy. | not designed | [Figma node](https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=955-17500) | RESOLVED | Resolved 2026-05-05 — **inline error banner inside the modal**, NOT sticky top flash. Verified z-index: modal root is `z-[2000]` (`modal.ex:63`) vs flash container `z-40` (`instructor_dashboard.html.heex:17`); flash would render behind modal backdrop = invisible. Reuse inline banner pattern from `student_support_parameters_modal.ex:150-155`: `<div :if={@error} class="rounded-md border border-Fill-Chart-fill-chart-red-active bg-Background-bg-primary p-3 text-sm leading-5 text-Text-text-high" role="alert">...</div>` rendered at top of modal body (above subject field). Modal stays open per PRD §8 reliability; Generate New Draft button re-enabled for retry. **Copy:** reuse G-J08 interim — "We couldn't generate a draft right now. Try again or write your own." (Final wording polished with Jess before v1.) |
| G-D05 | Validation error UI for Send-time placeholder validation (Jess + Darren explicit ask). Inline per-field? Banner? Block button + tooltip? | not designed | [Figma node](https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=955-17500) | RESOLVED | Resolved 2026-05-05 by Jess (Slack confirmation: _"yes this all sounds fine!"_). **Field-bound inline error pattern** — error renders directly under the offending field (subject OR body) using `student_support_parameters_modal.ex:305-315` style: `text-xs leading-4 text-Fill-Chart-fill-chart-red-active`. Validator returns errors keyed by field (e.g., `%{subject: [msg], body: [msg]}`). **Empty-field validation** is handled by Send-button-disabled state (`send_disabled?/1` at `student_support_email_modal.ex:312-316`). **Resolver enhancement:** add optional `fallback: :email` keyword arg to `user_name/1` (and similar resolvers) — default preserved for existing callers; MER-5257 opts in via `user_name(recipient, fallback: :email)` so `{student_name}` falls back to email when User has no name. **Final error wording:** _"Unsupported placeholder `{firstname}`. Allowed: `{first_name}`, `{student_name}`, `{instructor_name}`, `{course_name}`."_ |
| G-D06 | Empty state — zero chips, empty subject, empty body (e.g., before AI generation completes). | not designed | [Figma node](https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=955-17500) | RESOLVED | Resolved 2026-05-04 from sibling patterns. Zero chips: contextual help text below "To:" field — `<p :if={@recipient_students == []} class="text-sm leading-5 text-Text-text-low-alpha">No students currently need this message...</p>` (matches `student_support_email_modal.ex:103-108`). Empty subject/body: rely on `placeholder` attribute + Send button disabled via `send_disabled?/1` logic (matches `student_support_email_modal.ex:312-316`). No custom empty-state component needed. |
| G-D07 | "+N more" expanded chip overflow pattern — Jira a11y notes mention it. Figma shows only a `...` truncation pill, no expanded view. | partially designed | [Figma node](https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=955-17500) | RESOLVED | Resolved 2026-05-04 — pattern already implemented. `OverflowChipList` hook at `assets/src/hooks/overflow_chip_list.ts` (218 lines). Already wired into `student_support_email_modal.ex:65` via `phx-hook="OverflowChipList"`. Behavior: collapsed shows chips on one line + `+N more` toggle; expanded shows all chips + `Show less`. Responsive (resize observer), accessible (`aria-expanded`, `aria-label`). Reuse as-is. |
| G-D08 | "Add manually" recipient input affordance — Jira AC says "click into the field to add additional email recipients manually" but Figma does not show an input cell. | not designed | [Figma node](https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=955-17500) | RESOLVED | Resolved 2026-05-05 (cascading from G-J07 RESOLVED). Scope = section-enrolled students only (per Jess). **UI pattern:** "+ Add recipients" button rendered below the chip list opens a popover anchored to the trigger, hosting a section-scoped student picker (typeahead text input + scrollable checkbox list). Reuses checkbox-multiselect pattern from `student_support_tile.ex`. Selecting a student appends to recipient chip list; closing the popover commits selection. No free-text email entry. Risks to mind during implementation: popover positioning inside modal (modal `z-[2000]` requires popover to be portaled or offset correctly), section roster size (paginate or virtual-scroll if > ~200 students). |
| G-D09 | Hyperlink insert / edit UI in body — Jira AC requires it but Figma shows a plain textarea with no toolbar or affordance. | **Partially designed.** Sibling node `1115:18333` ("Assignment Email") body shows a rendered hyperlink (`[link to "Gardening Pretest"]` styled as a link) — confirms hyperlinks are intended in body output. Insert/edit affordance (toolbar, link dialog, keyboard shortcut) is still NOT designed in either node. | [Sibling Figma node 1115:18333](https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=1115-18333) | RESOLVED | Resolved 2026-05-05 — **reuse Slate `RichTextEditor` restricted to inline + link only.** Slate is the canonical content-authoring editor in this codebase (`assets/src/components/content/RichTextEditor.tsx`, used in activity stems, feedback, popups, curriculum modals). Existing link insert pattern lives at `assets/src/components/editing/elements/link/{LinkCmd.tsx,LinkModal.tsx,LinkElement.tsx}`. Implementation must FOLLOW existing usage, not reinvent: read existing call sites (e.g., `MultiInputStem.tsx`, `FeedbackCard.tsx`, `PopupContentEditor.tsx`, `options_modal.ex`) to understand toolbar config, hook bridge between LiveView and React, serialization to HTML via `assets/src/data/content/writers/html.tsx`, and modal mount pattern. Restrict configuration to: no block elements (paragraph + text + link only), no math/embeds/images/code/tables, toolbar limited to "Insert Link" button. Send pipeline: Slate JSON → HTML (existing serializer) for `html_body`; plain-text fallback for `text_body` via Premailex. AI prompt instructs output as markdown links `[label](url)` deserialized to Slate JSON on render. **FR-014 (Hyperlink insert/edit) is BACK IN SCOPE for v1.** |
| G-D10 | "Email sent" confirmation banner — Jira AC says displayed in the dashboard. Position, duration, animation, dismissibility, location relative to the originating tile not specified. | not designed | [Figma node](https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=955-17500) | RESOLVED | Resolved 2026-05-04 — reuse existing flash infrastructure. Pattern: `send(self(), {:flash_message, {:info, "Email sent"}})` from email modal → parent LiveView handler (`instructor_dashboard_live.ex:1169`) calls `put_flash(socket, :info, "Email sent")` → flash renders via `common.ex:798-833` in sticky top container (`layouts/instructor_dashboard.html.heex:17`, z-40). Manually dismissible via close button. **No auto-dismiss currently implemented**; if design wants 3-5s fade, extend flash component with optional `auto_dismiss_ms` attribute — flag to design as a polish item. |
| G-D11 | Mobile / responsive variant — see G-J09 for scope question. If in scope, design needed. | not designed | [Figma node](https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=955-17500) | RESOLVED | Resolved 2026-05-04 — adopt sibling pattern. Match `student_support_parameters_modal.ex:51-52` responsive padding: `px-6 pt-6 pb-4 md:px-10 md:pt-8 lg:px-16 lg:pt-16`. Existing `student_support_email_modal.ex:37-38` static padding (`px-[38px] pt-[30px]`) is the outlier — do not propagate. See also G-J09 RESOLUTION for sibling tile precedent. |
| G-D12 | Cancel button — visible in Figma but not enumerated in Jira AC. Confirm canonical: keep both Cancel and Send, or treat Cancel as redundant with X close? | Figma has Cancel; Jira AC mentions only X close + Send | [Figma node](https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=955-17500) | RESOLVED | Resolved 2026-05-04 — Cancel button is canonical. Established pattern in `student_support_parameters_modal.ex:162-185` (Cancel secondary + Save primary) and current `student_support_email_modal.ex:165-194` (Cancel + Send). Keep Cancel as secondary variant in custom footer alongside Send (primary). Treat Jira AC silence as omission, not exclusion. |
| G-D13 | Send button rendered twice in the Figma layer tree — once at bottom-right with icon, once apparently inside the body wrapper. Likely a Figma layer error. Confirm bottom-right with Send icon as canonical. | Figma layer artifact | [Figma node](https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=955-17500) | RESOLVED | Resolved 2026-05-04 — single Send button canonical at bottom-right with `Icons.send` (h-5 w-5), primary variant, size sm. Matches shipping `student_support_email_modal.ex:177-194` and sibling `student_support_parameters_modal.ex` Save button position. Figma duplicate is a layer artifact; ignore. |
| G-D14 | Subject input — Figma shows single-line with ellipsis truncation. Confirm vs multi-line wrap. | Figma single-line | [Figma node](https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=955-17500) | RESOLVED | Resolved 2026-05-04 — keep single-line `<input type="text">` matching `student_support_email_modal.ex:133-142` (h-[40px], rounded-[6px], px-4, text-base/leading-6). HTML inputs are inherently single-line. If Figma requires visible ellipsis on overflow, use inline style `overflow: hidden; text-overflow: ellipsis; white-space: nowrap;` (Tailwind `truncate` does not apply to `<input>` — only block/flex elements). |

## B3 — Token drift (owner: design team)

Figma node URL: https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=955-17500
Token file: `assets/tailwind.tokens.js`

Codebase token names match Figma names but resolved hex values differ. Per `~/.claude/rules/figma-to-code.md`: "Token names matching does NOT mean colors match. Token files can drift out of sync with Figma. If the values differ, update `tailwind.tokens.js` to match Figma." However, given the high blast radius of token edits (e.g., `fill-primary` is used by every primary button across the app), defer the change until design confirms which value is canon.

| ID | Token | Figma hex (resolved) | Codebase dark hex | Codebase light hex | Notes | URL | Status | Decision |
|----|-------|---------------------|-------------------|-------------------|-------|-----|--------|----------|
| G-T01 | `Fill-Buttons-fill-primary` | `#0062F2` | `#0080FF` | `#0080FF` | Critical button blue. Codebase value is identical for both modes. Used everywhere primary buttons render — high blast radius. Drift visible on Generate New Draft button ([node 955:17524](https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=955-17524)) + Send button ([node 955:17546](https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=955-17546)). | [Generate New Draft 955:17524](https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=955-17524) / [Send 955:17546](https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=955-17546) | RESOLVED | Resolved 2026-05-05 — **add a NEW token, do not overwrite existing one.** Add `Fill-Buttons-fill-primary-bold` to `assets/tailwind.tokens.js` with `light: '#0062F2', dark: '#0062F2'`. MER-5257 Generate New Draft + Send buttons use the new token; existing `Fill-Buttons-fill-primary` (#0080FF) stays untouched (blast radius across the app is too large to flip globally without design-system review). **Question for design/Jess to raise later:** should all primary buttons across the app eventually move to `#0062F2` (then update the existing `Fill-Buttons-fill-primary` token globally and retire the new bold variant), or is the bold variant intentional only for the email modal's hero actions? Defer this question — does not block MER-5257 implementation. |
| G-T02 | `Specially-Tokens-Border-border-input` | `#3E3F44` | `#2B282E` | `#CED1D9` | Figma drift appears only on the scrollbar track ([Bar node 955:17542](https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=955-17542)) — browser/OS-rendered surface, not directly styled in design system. | [Bar node 955:17542](https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=955-17542) | RESOLVED | Resolved 2026-05-05 — not a real drift. The `#3E3F44` value only appears on the scrollbar track in Figma, which is browser/OS-rendered in the actual app. Design tokens for inputs are unaffected. Codebase `Specially-Tokens-Border-border-input` stays as-is (light `#CED1D9`, dark `#2B282E`). No token change needed. |
| G-T03 | `Fill-Accent-fill-accent-grey-muted` | `#B0B4BF` | `#3B3740` | `#B0B4BF` | The Figma resolved value matches the codebase **light** hex but is used in a dark context (scrollbar thumb on dark page). Either token is intentionally light-only, or codebase dark value is wrong, or Figma is using the token cross-mode. | [Figma node](https://www.figma.com/design/2DZreln3n2lJMNiL6av5PP/Instructor-Intelligent-Dashboard?node-id=955-17500) | RESOLVED | Resolved 2026-05-04 — codebase token is correct as-is. The `#B0B4BF` light value is reused in the dark-mode Draft Email scrollbar intentionally for high-contrast visibility (light grey on dark background). The codebase dark value `#3B3740` remains correct for general dark-mode usage of this token elsewhere. No token file change required. Implementation: apply the light-variant hex directly for the scrollbar thumb in the dark theme, or use a dedicated scrollbar-thumb class if available. |

## Naming inconsistency (informational, not a blocker)

- Figma variable namespace: `Border/border-input`
- Codebase namespace: `Specially-Tokens-Border-border-input` (under specialty, not generic Border)
- Class still resolvable via codebase name. Worth flagging to design system maintainers.

## How this list should be used

1. The implementer surfaces this entire document in a single batched ask to the relevant owners (Jess for B1, design team / Jess for B2 + B3).
2. As answers arrive, status flips `OPEN` → `ASKED` → `ANSWERED` → `RESOLVED`.
3. Once `ANSWERED`, the corresponding entry is applied to `prd.md` / `requirements.yml` / `fdd.md` / code, then status flips to `RESOLVED`.
4. Items that turn out to be out of scope this iteration are moved to `## Deferred` at the bottom of this file with a one-line rationale.

## Deferred

(none yet)
