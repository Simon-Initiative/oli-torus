defmodule Oli.IndependentLearnersTest do
  use Oli.DataCase
  alias Oli.Delivery.Sections

  describe "" do
    setup do
      # is_independent_learner
      # is_independent_instructor

      # other file:
      # is_institution_instructor

      # enroll_independent
      # unenroll students


      # test author ability to create sections (can_create_sections)

      # changing platform roles (user_detail_view)

      # other test: maybe_enroll_open_and_free_user plug
      # require_independent_instructor
    end
  end
end
