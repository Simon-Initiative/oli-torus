defmodule Oli.SectionInvitesTest do
  use Oli.DataCase
  alias Oli.Delivery.Sections

  describe "" do
    setup do
      # slug, section_id, date_expires is required
      # link is valid

      # get_section_invite
      # list_section_invites
      # create_default_section_invite
      # is_link_valid?

      # expire_after :one_day, :one_week, :section_start, :section_end
    end
  end
end
