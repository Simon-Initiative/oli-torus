apiVersion: v1
kind: ResourceQuota
metadata:
  name: preview-quota
  namespace: ${PR_NAMESPACE}
spec:
  hard:
    requests.cpu: "1000m"
    requests.memory: 6Gi
    limits.cpu: "2000m"
    limits.memory: 8Gi
    persistentvolumeclaims: "2"
    requests.storage: 20Gi
