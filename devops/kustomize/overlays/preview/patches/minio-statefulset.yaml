apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: minio
spec:
  template:
    spec:
      containers:
        - name: minio
          env:
            - name: MINIO_SERVER_URL
              value: https://pr-$(PR_NUMBER).$(PREVIEW_DOMAIN)/minio
            - name: MINIO_BROWSER_REDIRECT_URL
              value: https://pr-$(PR_NUMBER).$(PREVIEW_DOMAIN)/minio
